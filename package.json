{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^1.7.3"
  },
  "scripts": {
    "start": "node mocksocket.js",
    "cypress": "cypress",
    "test": "node mocksocket.js & pid=$!; npm run cypress run; kill -9 $pid",
    "open": "node mocksocket.js & pid=$!; npm run cypress open; kill -9 $pid"
  },
  "devDependencies": {
    "cypress": "^3.2.0"
  }
}
